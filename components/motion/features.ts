// Loaded lazily by <MotionProvider> so Motion's animation engine stays out
// of the critical bundle. domMax (not domAnimation) for layoutId highlights.
import { domMax } from "motion/react";

export default domMax;
